import { buttonImage } from "./buttonImage";
import { onClickMethod } from "./searchMethod";

export const searchProp = {
	placeholder: 'Search what you want',
	buttonLabel: 'Search',
	onClickMethod: onClickMethod,
	showImage: true,
	image: buttonImage
}